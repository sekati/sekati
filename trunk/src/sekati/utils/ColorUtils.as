/** * sekati.utils.ColorUtils * @version 1.0.0 * @author jason m horwitz | sekati.com * Copyright (C) 2008  jason m horwitz, Sekat LLC. All Rights Reserved. * Released under the MIT License: http://www.opensource.org/licenses/mit-license.php */package sekati.utils {	import flash.display.DisplayObject;	import flash.geom.ColorTransform;		/**	 * ColorUtils	 */	public class ColorUtils {		/**		 * Generate a random hexidecimal color.		 */		public static function randColor() : * {			return  Number( "0x" + Math.floor( Math.random( ) * 16777215 ).toString( 16 ).toUpperCase( ) );		}		/**		 * Set color of a movieclip or textfield		 */		public static function setColor(o : DisplayObject,hex : Number) : void {			var ct : ColorTransform = o.transform.colorTransform;			ct.color = hex;			o.transform.colorTransform = ct;		}		/**		 * get color from a movieclip or textfield		 */		public static function getColor(o : DisplayObject) : Number {			return o.transform.colorTransform.color;		}		/**		 * Converts the string passed in argument (the html color) in a number representation.		 */		public static function htmlToNumber( sHTML : String ) : Number {			if ( sHTML.charAt( 0 ) == "#" && sHTML.length > 1 && sHTML.length <= 7) {				return parseInt( "0x" + sHTML.substr( 1 ) ) ;			}			return 0 ;		}		/**		 * Converts the number passed in argument (the html color in hex with ECMAScript notation <code>0xrrggbb</code>) in a HTML string representation.		 */		public static function hexToHtml( hex : Number) : String {			return "#" + (hex.toString( 16 )).toUpperCase( ) ; 		}				/**		 * ColorUtils Static Constructor		 */		public function ColorUtils() {			throw new Error( "ColorUtils is a static class and cannot be instantiated." );		}	}}